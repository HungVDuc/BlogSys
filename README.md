Chạy project
1. Build và start
docker-compose up --build

2. Kiểm tra container
docker ps


API chạy tại:

http://localhost:3000

Dừng project
docker-compose down


Xóa luôn database:

docker-compose down -v